apiVersion: v1
kind: Service
metadata:
  name: {{ include "moleculer-faas.fullname" . }}
  labels:
    {{- include "moleculer-faas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moleculer-faas.selectorLabels" . | nindent 4 }}
