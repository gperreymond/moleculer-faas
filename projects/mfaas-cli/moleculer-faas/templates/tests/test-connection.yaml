apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moleculer-faas.fullname" . }}-test-connection"
  labels:
{{ include "moleculer-faas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moleculer-faas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
